[pytest]
# Pytest Konfiguration für Reservierungen API - Unit Tests

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Coverage
addopts = 
    --tb=short
    --disable-warnings
    -v

# Asyncio
asyncio_mode = auto
